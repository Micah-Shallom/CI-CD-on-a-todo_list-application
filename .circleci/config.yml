version: 2.1
orbs: 
  versioning: streamroot/versioning@0.1.1
jobs:
  build:
    docker:
      - image: alpine/git
    steps:
      - checkout
      - versioning/generate

workflows:
  my_workflow:
    jobs:
      - build
