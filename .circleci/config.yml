version: 2.1
orbs:
  docsync: streamroot/versioning@1.0.0
jobs:
  build:
    docker:
      - image: alpine/git
    steps:
      - checkout
      - versioning/generate

workflows:
  my_workflow:
    jobs:
      - build
